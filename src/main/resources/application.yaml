app:
  jwt:
    secret: PPY1999


spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/fitnessbd
    password: admin
  jpa:
    generate-ddl: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    properties:
      hibernate:
        format_sql: 'true'
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true

